#include "minishell.h"

echo "$USER" $$'$PATH' > file | cat  << limiter > file | cat < file >> out_file

t_token *_lexer(char *input)
{
	t_token *head;
	t_token *current;
    int     i;

    i = 0;
    while(input[i]) {
        
    }


}